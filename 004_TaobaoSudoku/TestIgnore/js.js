/*
window.load=function(){
	// 计算鼠标的位置
	// 计算两框的比例 求位置
}


function getMousePos(event) {
      var e = event || window.event;
      return {'x':e.clientX,'y':clientY}
}

*/
window.load=function(){
	function mouseHover (){
		// 选择器
	function $(el) {
		return docment.querySelector(el)
	}
	// 获取元素
	let container = $()
	let leftView = $()
	let mask = $()
	let smallImag = $()
	let rightView = $()
	let bigImg = $()

}
}

